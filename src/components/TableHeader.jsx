import React from 'react';

function TableHeader({
    path,
    title,
    columnResizeStart,
    setResizing,
    dispatchers,
    addSeparator,
    sortOrder
}) {
    return <th>

    </th>;
}

export default TableHeader;
